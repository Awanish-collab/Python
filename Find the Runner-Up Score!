'''
  Given the participants' score sheet for your University Sports Day, you are required to find the runner-up score. You are given  scores. 
  Store them in a list and find the score of the runner-up.
  Find the Runner-Up Score!  means  finding the 2nd maximum number from the array.
  
  Sample Input :   5
                   2 3 6 6 5
                   
  Sample Output :  5

'''


n = int(input())
arr = map(int, input().split())
b = sorted(set(arr))
print(b[-2])
